# Authorization receipt

On 2026-09-02, Brandon Music explicitly authorized V2 of the local GLM-5.3-Flash BF16-to-NVFP4 plan, local downloads, local GPU execution, automatic continuation, and creation of a Codex goal to fully implement the plan. This does not authorize uploads, publication, destructive cleanup outside the campaign root, service-state drift, or changes to GPU power limits.

On 2026-09-03, Brandon Music explicitly continued and expanded that authorization: implement and test runtime-correct in-block Hadamard-16 and learned orthogonal rotations from the BF16 source, measure end-to-end teacher-logit KLD, iterate under sealed selection rules until a rotation candidate beats stock NVFP4, and then implement and measure a Shapley-based global allocation at 6.0 routed-expert bpw. Local downloads, use of the four local GPUs, automatic continuation, and non-destructive campaign-local artifact creation are authorized. Upload, publication, destructive cleanup outside the campaign root, changes to unrelated services, and GPU power-limit changes remain unauthorized.
