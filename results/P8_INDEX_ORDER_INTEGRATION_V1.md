# Corrected-index full-model forced-decode canary v1

Status: design before serving execution. This stage returns to full-model
numerical closure after the five-process synthetic kernel pass; it is not
another encoder screen or a substitute for the conditional-fit32 KLD panel.

The frozen plan is `experiments/p8-index-order-integration-v1.json`, SHA256
`5514b3f29db3aad7e88327f1af336013b35f951aa49c96d3846bd0d0a7833e0f`.
It binds 52 source files. Before launch, 163 focused CPU tests passed, the
exact-image serving-import receipt verified, and independent lifecycle review
found no blocking issue. These checks are not integrated GPU results.

## Evidence and intervention

The device-v2 root receipt
`251757fd24931fe2ca260fedd63ed1a3819aa5170194b0298dc95036c33390f5`
records five new GPU0 processes,2,300 declared checks, and identical short
output-byte signature
`d7592d778e06f037de18ae43ad5db4be33ef2f04239ab5c8727db2d564fe6772`.
An independent verifier reconstructed all analytic score-pair hashes and700
candidate ordered-output hashes. Those are receipt/reference-hash checks,
not rereading raw device arrays, which were not retained. Peak sampled35C;
owned containers removed, prior services restored. The stopped v1 UUID-format
attempt remains failed and is not counted among these five processes.

The unchanged candidate fused-indexer source emits SHA256
`655236be67b31d61a159fcce01ad85fe74b9c2445aedcbac3b474fb664e6de86`.
Its valid short-row logical placement preserves score/index pairs and is
restricted to paged logical32-head K512 cross-CTA kernels. The original
source is69110dcf9d54d4e14ee4d501990245a2cbad7621d0a3d84f035f93d368add7af.
No trellis weights, encoder, P8 arithmetic, MLA cache format, causal tokens or
tail-token policy change. A one-time per-rank receipt proves the corrected
public indexer call is reached without the prior143-copy-kernel observer.

## Predeclared order and exact decision

Use three fresh TP4/noEP/DCP1 serving processes, image
`sha256:0f1eae9329965d68713857e4a5a12e9c5440c866b532e7ba288dc2ae4067fad9`,
Model Runner V2, FULL CUDA graphs, NVFP4 MLA KV, all42 routed P8 layers and the
same already-opened conditional-fit0056 causal sequence:

1. Corrected indexer plus original N128 FC1/separate scratch; capture all2047
   forced-M1 pre-mask logit rows,154880 real-vocabulary columns.
2. A second fresh process with exactly the same settings. Every raw logit
   byte must equal process1. Any difference stops before process3 and remains
   a failure, even if the first difference moves later than row259.
3. Corrected indexer plus the optimized N64 FC1/consolidated scratch. Every
   captured logit byte must equal both N128 captures. No numeric tolerance,
   row exclusions or post-result threshold changes are allowed.

The experimental unit is a fresh serving process. Tokens/layers/ranks are
correlated subsamples. This is an exact deterministic canary, not an estimate
of population quality or a generalization confidence interval. Two matching
full-model repeats do not alone satisfy the final five-run serving-determinism
requirement. The synthetic five-process evidence cannot substitute for it.

Required receipts include corrected-index invocation on every TP rank,
four-rank FULL graph capture, V2 warmup/capture contract, actual M1 dispatch
on all42 layers, complete causal token alignment, captured data hashes and
unchanged pinned source/weight identities. No protected inputs or teacher
logits are opened. No KLD is computed here; full32 measurement remains next
only after this prerequisite passes and its separate plan is frozen.

## Safety, failure and scope

Use the existing maintenance lock and authenticated container lifecycle.
Preserve three captures requiring3,804,472,320 raw bytes plus20GiB headroom.
Start at or below75C and abort at or above90C. Every attempt has a fresh
namespace; no retry/resume or failed-row replacement. Restore prior services
only after safe owned-container shutdown. Capture overhead disqualifies all
observed throughput as speed evidence.

The separate incomplete-KPool-tail omission remains unresolved and unchanged.
Passing here localizes/fixes the observed repeatability defect only; it does
not establish that the attention path equals a correct BF16 model. A separate
tail repair and its quality impact must be explicitly evaluated before final
quality qualification. Other rivals include an unobserved downstream
non-repeatability source or real differences between N128 and N64 arithmetic.

P8 remains procedural trellis-to-E4M3, with twice NVFP4's MMA issue count.
B12X supplies the original scorer/relay and serving API; existing ExLlamaV3,
KQuant, QSRT and w4a8_trellis attribution is retained. No new speed, KLD,
allocation or P4/NVFP4-class claim follows from this canary design.
